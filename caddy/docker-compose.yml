networks:
  web:
    external: true

services:
  caddy:
    image: caddy:alpine@sha256:11aadc735925d782bdc5d83344408a9d630ba00cf45e68108e671011bc7386bc
    container_name: caddy
    command: caddy run --watch --config /etc/caddy/Caddyfile --adapter caddyfile
    restart: always
    configs:
      - source: Caddyfile
        target: /etc/caddy/Caddyfile
    environment:
      - ETESYNC_DOMAIN=(ETESYNC_DOMAIN)
    ports:
      - 80:80
      - 443:443
      - 443:443/udp
      # - "8448:8448"
    volumes:
      - /home/fzr/caddy/configs:/etc/caddy/configs:ro
      - /home/fzr/caddy/well-known.json:/static/client
      - data:/data
      - config:/config
    networks:
      - web

configs:
  Caddyfile:
    file: /home/fzr/caddy/Caddyfile

volumes:
  data:
  config:
